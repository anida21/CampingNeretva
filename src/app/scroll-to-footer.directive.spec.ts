import { ScrollToFooterDirective } from './scroll-to-footer.directive';

describe('ScrollToFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToFooterDirective();
    expect(directive).toBeTruthy();
  });
});
